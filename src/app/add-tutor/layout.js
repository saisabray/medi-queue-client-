export const metadata = {
  title: "MediQueue | Add Tutor",
  description:
    "Add a new tutor to the MediQueue platform.",
};

export default function AddTutorLayout({ children }) {
  return <div>{children}</div>;
}
